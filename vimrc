" Set the default tab width to 4 spaces
set tabstop=4       " Number of spaces that a <Tab> in the file counts for
set shiftwidth=4    " Number of spaces to use for each step of (auto)indent
set expandtab       " Use spaces instead of tabs for indentation

" Enable both absolute and relative line numbers
set number               " Show absolute line numbers
set relativenumber       " Show relative line numbers in addition to absolute numbers

" Change the page down key binding to Ctrl+F
nnoremap <C-f> <PageDown>
inoremap <C-f> <PageDown>
vnoremap <C-f> <PageDown>

" Change the page up key binding to Ctrl+G
nnoremap <C-g> <PageUp>
inoremap <C-g> <PageUp>
vnoremap <C-g> <PageUp>

